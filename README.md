# GoogleNews (RSS reader)

The project uses:

 * NSXMLParser to parse downloaded data,
 * UITableView to display a short list of news items (titles and icons),
 * UIWebView to display the full article.
